# Conditionals Homework

## Question 1
Priates like to use luck sometimes to make decisions. Build a method called `should_we_sail?` that takes in a number from 1 to 6, if the number is 1, 2 or 3 then return `true` otherwise return `false`.

## Question 2
Write a method called `roll_dice` that return a random number between `1` and `6`. Write a code to pass the output of `roll_dice` as an argument to the `should_we_sail?` method.